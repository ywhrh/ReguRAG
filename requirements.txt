# LangChain core and community integrations
#pip install "ragas==0.4.3" "langchain-core>=1.4.7"
#In ragas 0.4.3, a widely reported bug causes evaluations and imports to crash with a ModuleNotFoundError: No module named 'langchain_community.chat_models.vertexai'
langchain>=1.3.0
langchain-community>=0.4.0

# Anthropic Claude API
anthropic>=0.40.0

# Chroma vector store integration
langchain-chroma>=1.1.0
chromadb>=0.5.0

# Local embedding model via Hugging Face sentence-transformers
langchain-huggingface>=1.0.0
# sentence-transformers 3.x requires torch>=2.4, which is unavailable on
# some platforms, so keep the 2.x line and compatible numpy/transformers.
sentence-transformers>=2.7.0,<3.0.0
transformers>=4.35.0,<5.0.0
numpy>=1.24.0,<2.0.0

# PDF document loading
pypdf>=4.0.0

# Text splitting
langchain-text-splitters>=1.1.0

# Environment variable loading
python-dotenv>=1.0.0

# Web UI
streamlit>=1.35.0
